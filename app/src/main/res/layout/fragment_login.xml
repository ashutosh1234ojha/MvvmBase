<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="handler"
            type="com.ashutosh1234ojha.mvvmbase.views.LoginFragment" />

        <variable
            name="loginViewModel"
            type="com.ashutosh1234ojha.mvvmbase.viewmodels.LoginViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="10dp">

        <androidx.appcompat.widget.AppCompatEditText
            android:id="@+id/etEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="20dp"
            android:hint="@string/email"
            android:padding="6dp"
            android:text="@={loginViewModel.user.email}"
            app:layout_constraintBottom_toTopOf="@+id/etPassword" />

        <androidx.appcompat.widget.AppCompatEditText
            android:id="@+id/etPassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="20dp"
            android:hint="@string/password"
            android:padding="6dp"
            android:text="@={loginViewModel.user.password}"
            app:layout_constraintBottom_toTopOf="@+id/guideCenter" />

        <!--        <Button-->
        <!--            android:id="@+id/btnSignIn"-->
        <!--            android:layout_width="match_parent"-->
        <!--            android:onClick="@{handler::runCode}"-->
        <!--            android:layout_height="wrap_content"-->
        <!--            android:text="@string/sign_in"-->
        <!--            app:layout_constraintTop_toBottomOf="@+id/guideCenter" />-->

        <Button
            android:id="@+id/btnSignIn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{()->loginViewModel.handleLogin()}"
            android:text="@string/sign_in"
            app:layout_constraintTop_toBottomOf="@+id/guideCenter" />

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/guideCenter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layout_constraintGuide_percent=".5" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
